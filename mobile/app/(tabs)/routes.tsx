import { View, Text } from 'react-native';

export default function RoutesScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-xl font-bold text-primary">Routes</Text>
    </View>
  );
}
